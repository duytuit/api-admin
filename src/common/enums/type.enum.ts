export enum TypeCategoryEnum {
  or = 0,
  and = 1,
}
export enum ProductEnum {
  single_entry = 1,
  multiple_entry = 2,
}

export enum CustomerEnum {
  visa = 1,
}

export enum CategoryEnum {
  visa = 1,
  post = 2,
}
